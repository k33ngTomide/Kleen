spring:
    datasource:
      url: jdbc:postgresql://localhost:5432/kleen_db
      username: Tomide
      password: A#1234MuiliyuSodiq
      driver-class-name: org.postgresql.Driver
    jpa:
      hibernate:
        ddl-auto: create
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect


logging:
  level:
    org.springframework.security: DEBUG

